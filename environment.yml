name: no2-forecasting
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pytorch
  - torchvision
  - pytorch-cuda=12.1        # remove for CPU-only
  - numpy>=1.26
  - pandas>=2.0
  - netCDF4>=1.7
  - matplotlib>=3.9
  - scikit-learn>=1.4        # for future metrics / preprocessing
  - jupyter
  - ipykernel
  - tqdm
