# Python
__pycache__/
*.py[cod]
*.pyo

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Model checkpoints (large binary files — keep out of git)
outputs/*.pt

# Keep plots, JSON results, and CSVs in git
# outputs/*.png  — tracked
# outputs/*.json — tracked
# outputs/*.csv  — tracked

# Env / IDE
.env
.venv/
.vscode/
*.egg-info/

# Data (large NetCDF files live outside the repo)
*.nc
data/*.nc
